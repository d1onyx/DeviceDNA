<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- ── Identity (Xcode substitutes build settings) ─────────────────── -->
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleDisplayName</key>
	<string>DeviceDNA</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>arm64</string>
	</array>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UILaunchScreen</key>
	<dict/>

	<!-- ── Compose Multiplatform requirement ─────────────────────────────
	     Without this, ComposeUIViewController's PlistSanityCheck deliberately crashes
	     the app at launch (abort() from terminateWithUnhandledException) so ProMotion
	     (120Hz) devices don't silently get throttled animations. ── -->
	<key>CADisableMinimumFrameDurationOnPhone</key>
	<true/>

	<!-- ── Deep links: widgets and notifications open devicedna://open/<route> ── -->
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleURLName</key>
			<string>$(PRODUCT_BUNDLE_IDENTIFIER).deeplink</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>$(IOS_DEEP_LINK_SCHEME)</string>
			</array>
		</dict>
		<dict>
			<!-- Google Sign-In callback. GIDSignIn.signIn(withPresenting:) crashes with an
			     NSException at launch of the sign-in flow if this scheme isn't registered —
			     value is REVERSED_CLIENT_ID from GoogleService-Info.plist. -->
			<key>CFBundleURLName</key>
			<string>$(PRODUCT_BUNDLE_IDENTIFIER).google</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>$(GOOGLE_REVERSED_CLIENT_ID)</string>
			</array>
		</dict>
	</array>

	<!-- ── Background refresh (battery history, widgets, smart alerts) ─── -->
	<key>UIBackgroundModes</key>
	<array>
		<string>fetch</string>
	</array>
	<key>BGTaskSchedulerPermittedIdentifiers</key>
	<array>
		<string>$(IOS_BACKGROUND_TASK_ID)</string>
	</array>

	<!-- ── AdMob ─────────────────────────────────────────────────────────
	     Test application id — replace with the production GADApplicationIdentifier
	     from the AdMob console before submitting. -->
	<key>GADApplicationIdentifier</key>
	<string>$(IOS_ADMOB_APP_ID)</string>

	<!-- ATT: shown by UMP only when consent requires tracking. Reviewers check that
	     this string honestly explains the purpose. -->
	<key>NSUserTrackingUsageDescription</key>
	<string>Your data will be used to deliver personalized ads and to measure advertising performance.</string>

	<!-- SKAdNetwork: privacy-preserving ad attribution without IDFA (Google's list;
	     the full current set ships with the Google Mobile Ads SDK documentation). -->
	<key>SKAdNetworkItems</key>
	<array>
		<dict>
			<key>SKAdNetworkIdentifier</key>
			<string>cstr6suwn9.skadnetwork</string>
		</dict>
		<dict>
			<key>SKAdNetworkIdentifier</key>
			<string>4fzdc2evr5.skadnetwork</string>
		</dict>
		<dict>
			<key>SKAdNetworkIdentifier</key>
			<string>2fnua5tdw4.skadnetwork</string>
		</dict>
		<dict>
			<key>SKAdNetworkIdentifier</key>
			<string>ydx93a7ass.skadnetwork</string>
		</dict>
		<dict>
			<key>SKAdNetworkIdentifier</key>
			<string>p78axxw29g.skadnetwork</string>
		</dict>
	</array>

	<!-- ── Usage descriptions ────────────────────────────────────────────
	     The app never captures photos/video or motion data; it only reads hardware
	     capability metadata. The strings still ship because the frameworks are linked
	     (AVFoundation camera enumeration, CoreMotion availability checks) and honest
	     descriptions preempt ITMS-90683 upload rejections. -->
	<key>NSCameraUsageDescription</key>
	<string>DeviceDNA reads camera hardware specifications (lens count, flash, stabilization) for the diagnostics report. It never captures photos or video.</string>
	<key>NSMotionUsageDescription</key>
	<string>DeviceDNA checks which motion sensors (accelerometer, gyroscope, barometer, pedometer) are present for the diagnostics report. It never records motion data.</string>

	<!-- ── Export compliance: HTTPS only (exempt) — skips the encryption questionnaire. -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>

	<!-- ── Sync backend base URL, injected from Config.xcconfig (SYNC_BASE_URL) ─── -->
	<key>SyncBaseUrl</key>
	<string>$(SYNC_BASE_URL)</string>
	<key>AppGroupId</key>
	<string>$(IOS_APP_GROUP_ID)</string>
	<key>BackgroundTaskId</key>
	<string>$(IOS_BACKGROUND_TASK_ID)</string>
	<key>DeepLinkScheme</key>
	<string>$(IOS_DEEP_LINK_SCHEME)</string>
	<key>StoreKitPremiumProductId</key>
	<string>$(IOS_STOREKIT_PREMIUM_PRODUCT_ID)</string>
	<key>AdMobBannerAdUnitId</key>
	<string>$(IOS_ADMOB_BANNER_AD_UNIT_ID)</string>
	<key>AdMobInterstitialAdUnitId</key>
	<string>$(IOS_ADMOB_INTERSTITIAL_AD_UNIT_ID)</string>
</dict>
</plist>
